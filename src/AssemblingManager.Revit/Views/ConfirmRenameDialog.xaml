<Window x:Class="AssemblingManager.Revit.Views.ConfirmRenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переименование сборок"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AssemblingManager;component/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="SummaryText"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <TextBlock Grid.Row="1"
                   Text="Хотите переименовать виды для данных сборок?"
                   Margin="0,0,0,10" />

        <ScrollViewer Grid.Row="2"
                      MaxHeight="260"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="RenameListText"
                       TextWrapping="Wrap" />
        </ScrollViewer>

        <Button Grid.Row="3"
                Content="Да"
                Width="80"
                Margin="0,15,0,0"
                HorizontalAlignment="Right"
                IsDefault="True"
                Click="ButtonYes_Click"
                Style="{StaticResource PrimaryButton}" />

        <Button Grid.Row="4"
                Content="Отмена"
                Width="80"
                Margin="0,8,0,0"
                HorizontalAlignment="Right"
                IsCancel="True"
                Style="{StaticResource PrimaryButton}" />
    </Grid>
</Window>
