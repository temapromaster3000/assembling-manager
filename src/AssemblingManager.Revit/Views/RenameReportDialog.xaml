<Window x:Class="AssemblingManager.Revit.Views.RenameReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переименование сборок"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AssemblingManager;component/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Переименование завершено."
                   FontWeight="Bold"
                   Margin="0,0,0,12" />

        <TextBlock Grid.Row="1"
                   x:Name="SummaryText"
                   Margin="0,0,0,10" />

        <ScrollViewer Grid.Row="2"
                      MaxHeight="260"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="RenameListText"
                       TextWrapping="Wrap" />
        </ScrollViewer>

        <Button Grid.Row="3"
                Content="OK"
                Width="80"
                Margin="0,15,0,0"
                HorizontalAlignment="Right"
                IsDefault="True"
                IsCancel="True"
                Click="ButtonOK_Click"
                Style="{StaticResource PrimaryButton}" />
    </Grid>
</Window>
